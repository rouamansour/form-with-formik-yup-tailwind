import '../styles/globals.css' // Importez le fichier CSS global

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} /> // Rendu du composant de la page
}

export default MyApp
